# Skills repository

本仓库维护可移植的 Agent Skills，以及约束其元数据、文档和行为契约的验证工具。

## Language

**项目初始化**：
根据目标技术栈创建项目代码骨架，并建立开发基线的工作流。

**新项目初始化**：
在空目录或近空目录中创建项目代码骨架并建立开发基线。

**已有项目基线补齐**：
保留已有代码与项目结构，仅识别并补齐缺失开发基线的初始化模式。

**目标技术栈**：
由用户明确指定，或从项目证据中无歧义识别出的语言、运行时与框架组合；存在歧义时以用户确认为准。

**项目形态**：
用户指定的语言原生交付形态，例如库、命令行程序或服务；缺失时必须确认。

**项目代码骨架**：
由目标技术栈的官方初始化方式生成并清理示例内容后，仍可构建和测试的最小代码与目录结构。

**Scaffold smoke test**：
不表达业务语义、只证明项目代码骨架及测试入口能够工作的最小测试。

**开发基线**：
使项目能够以一致方式进行开发和验证的最小环境与检查集合。

**生态基线**：
当目标技术栈缺少唯一官方工具组合时，项目明确采用并锁定的一组第三方工具约定。

**质量门**：
由本地与持续集成共同执行、用于判断项目是否达到开发基线的检查集合。

**快速检查**：
提交前执行且不包含完整测试套件的项目级类型、编译或等价静态检查。

**完整检查**：
统一执行格式检查、lint、类型或编译检查、测试与构建的质量门。
