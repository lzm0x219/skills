{
  "schema_version": 1,
  "skill": "dsa-design",
  "automated": true,
  "execution": {
    "mode": "codex-cli",
    "runner": "scripts/run_behavior_evals.py",
    "description": "The runner uses an authenticated Codex CLI to execute prompts and scores results with deterministic output assertions."
  },
  "source_assertions": {
    "required_regex": [
      "Run the applicability gate first",
      "When there is no material DSA choice",
      "Implement only when the user explicitly asks for code to be written or changed"
    ],
    "forbidden_regex": [
      "(?i:repolex|analysistarget|analysisoptions|analysisreport|languagestat|resolvedmode)"
    ]
  },
  "cases": [
    {
      "id": "prose-edit-no-trigger",
      "category": "out-of-scope",
      "invocation": "implicit",
      "prompt": "Change the settings-page button copy from \"Save\" to \"Save changes\". Return only the replacement copy and do not modify files.",
      "expected": {
        "assertions": {
          "required_regex": ["Save changes"],
          "forbidden_regex": [
            "(?:DSA|data structure|algorithm|complexity)",
            "(?:data structure|algorithm).*option"
          ]
        }
      }
    },
    {
      "id": "simple-crud-no-forced-dsa",
      "category": "applicability-gate",
      "invocation": "implicit",
      "prompt": "Add a method to delete a user by unique ID from the existing in-memory user list. The data set is small and there are no new performance or persistence requirements.",
      "expected": {
        "assertions": {
          "required_regex": ["(?:keep|reuse|existing).*list"],
          "forbidden_regex": ["DSA\\s*choice", "Option\\s*[123]"]
        }
      }
    },
    {
      "id": "top-k-material-decision",
      "category": "positive-trigger",
      "invocation": "explicit",
      "prompt": "Design an interface that returns the top 100 highest-scoring records from the last hour over a continuously growing multi-tens-of-millions event stream. Writes far outnumber reads, and memory has a clear upper bound. Give options first and do not make material trade-offs for me.",
      "expected": {
        "assertions": {
          "required_regex": [
            "Top\\s*-?\\s*(?:K|100)",
            "exact",
            "(?:approximate|Space-Saving|Count-Min)",
            "O\\(",
            "(?:memory|space).*(?:bound|limit|unbounded)",
            "(?:I/O|IO|disk|database|external state)",
            "(?:maintain|implementation complexity|operations)",
            "(?:recommend|prefer).*(?:exact|approximate|Space-Saving|Count-Min)",
            "(?:when|if).*(?:better|prefer|overtake|switch)",
            "(?:need|please).*(?:decide|choose)|choose.*semantic"
          ],
          "forbidden_regex": [
            "```(?:bash|sh|zsh|python|ruby|javascript|typescript|java|kotlin|swift|go|rust|c|cpp|csharp|sql)(?:\\s|\\n)",
            "(?:directly|immediately).*(?:implement|ship|deploy)"
          ]
        }
      }
    },
    {
      "id": "delegated-choice-no-pause",
      "category": "positive-trigger",
      "invocation": "explicit",
      "prompt": "Design a query that returns the top 100 highest-scoring records from the last hour over a multi-tens-of-millions event stream. Choose the most suitable option under these constraints; internal data structures and algorithms are yours to decide. Give the final design and verification plan first, without code, and without asking me to choose again.",
      "expected": {
        "assertions": {
          "required_regex": [
            "(?:Space-Saving|Count-Min|ring bucket|min-?heap)",
            "O\\(",
            "(?:final design|recommend|adopt|use)",
            "(?:memory|space).*(?:bound|limit)",
            "verif"
          ],
          "forbidden_regex": [
            "(?:need|please)\\s+(?:you\\s+)?(?:decide|choose)",
            "wait.*(?:decision|choice)",
            "Option\\s*[123]"
          ]
        }
      }
    }
  ]
}
