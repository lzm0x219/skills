Keep the linear-scan baseline until evidence shows indexing meets a real constraint. First check result equivalence with differential tests, including updates and deletes. Compare input sizes from 100 to 100000, key distribution and skew, read/write ratios, index build and update costs.
Record hardware, runtime version and seed; warm up and repeat runs. Report median/P95 latency and peak memory using the same workload and measurement scope. Complexity predicts scaling but does not measure speedup. Tests and benchmarks have not run, so the result is a verification plan only. Stop once correctness and the relevant selection criteria are established.
