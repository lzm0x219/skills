Use a byte-weighted bounded cache. Count key/value bytes, metadata overhead, expiration indexes and in-flight buffers within 64 MiB. TTL controls freshness, not the capacity bound. Evict under the capacity policy before admitting values; oversized entries bypass caching. LRU hits update recency under synchronization.
Use same-key single-flight loading and a generation token. Invalidation advances the generation; an older load cannot publish into a newer generation. Clear failed or cancelled loads. Test zero capacity, oversized values, replacements, simultaneous expiry and the load-invalidate-return interleaving. No tests have been run.
