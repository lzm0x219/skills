Final design adopts minute ring buckets, each keeping the highest-scoring records in a min-heap of capacity 100; on query, merge active buckets and take Top-100. Per-event updates are O(log 100) and memory stays bounded. Verify tied scores, window boundaries, late events, and peak throughput.
