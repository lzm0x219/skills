Represent A depends on B as B -> A. Deduplicate edges, reject missing tasks, and validate the complete graph before executing any task. Kahn's residual includes both cycles and blocked downstream nodes; use DFS or SCC analysis to report an actual cycle witness.
For a valid DAG, maintain a min-heap of ready IDs and release successors on predecessor completion. Selecting the lexicographically smallest currently ready ID costs O(E + V log V), with O(V + E) storage. Test chains, diamonds, duplicate edges, missing tasks, self-loops, a cycle plus a downstream task and permuted input order. Execution is blocked until preflight succeeds.
