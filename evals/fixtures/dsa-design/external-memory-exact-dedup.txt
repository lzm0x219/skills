Use external sort and multi-pass merge for exact deduplication. Size chunks by bytes, leaving RAM for the 1 MiB maximum record, sorting overhead and bounded merge buffers. Bound merge fan-in and file descriptors; account for temporary disk and final output separately. Sorting avoids relying on uniform hash partitions; a hash-partition alternative needs a fallback for skew and oversized partitions.
Bloom filter positives require lookup in exact state, not unconditional deletion. Test duplicates across chunks, skew, maximum records, disk exhaustion and interrupted temporary-file cleanup. Compare a small sample's result with an in-memory exact set baseline. These are planned checks.
