The target is score ranking, not frequency estimation. Count-Min Sketch and Space-Saving track frequent keys, so they do not solve this record ranking directly. Use a min-heap of capacity 10 with a deterministic tie-break. Immutable records with no expiry permit an exact O(N log 10) scan and O(10) auxiliary entries. Compare small inputs with a full sort, including equal scores and fewer than ten records.
