Local routing alone cannot prove currency. From the repository root run `node skills/framework/mise/scripts/verify-official-docs-inventory.mjs --check` so it compares inventory pages against the current official `llms.txt` and `sitemap.xml` indexes; only after it passes can you say the recorded official entry points are still indexed.
