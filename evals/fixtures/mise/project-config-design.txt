Put team configuration in project-root `mise.toml` and do not change global defaults. First determine supported Node and Python ranges, then pin the project versions under `[tools]`; put only non-sensitive variables in `[env]`.

Define test as a discoverable, reproducible task with clear tool dependencies and working directory. For verification, after authorized install run the actual test command in a mise context and then inspect the task definition; do not assume the environment works merely because a config file exists.
