先确认 CI 使用项目级 `mise.toml`，并在受控平台上生成、审查和提交 `mise.lock`。用 `mise lock` 固定工具解析的版本与校验信息；不要把 lockfile 当成已验证的构建结果。

在 CI 设计中使用当前官方文档的方式加载 mise 环境，再实际运行 build 命令和项目测试。只有这些命令在目标 CI 环境成功，才能声明构建可复现；本轮不生成或修改工作流文件。
