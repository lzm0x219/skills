First confirm CI uses project-level `mise.toml`, and generate, review, and commit `mise.lock` on controlled platforms. Use `mise lock` to pin resolved tool versions and verification data; do not treat the lockfile as a verified build result.

In the CI design, load the mise environment the way current official docs describe, then actually run the build command and project tests. Only when those commands succeed in the target CI environment can the build be called reproducible; this round does not generate or modify workflow files.
