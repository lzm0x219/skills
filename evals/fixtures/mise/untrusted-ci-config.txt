Do not rely on detecting CI to auto-trust this uncontrolled `mise.toml`. For version resolution or lockfile updates on pull-request branches, follow current official Security/CI docs and run the applicable form of `mise lock --bump` under `MISE_SAFE=1` so the config cannot execute templates, hooks, tasks, or project environment injection.

Safe mode is only suitable for the resolution operations it allows; if normal CI must run tasks or load environment, review the configuration first and follow the team's explicit trust policy. This round does not modify CI files.
