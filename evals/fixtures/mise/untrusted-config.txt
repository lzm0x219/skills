First review this `mise.toml` and any env, hook, template, and task files it references to determine whether they execute code, access the network, read credentials, or rewrite files. Until the source, content, and expected side effects are clear, do not run `mise trust` and do not mark unknown configuration as trusted.

After the check, confirm the trust scope with the owner; only after authorization run trust operations on the specified path.
