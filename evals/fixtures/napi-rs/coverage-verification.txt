The local inventory is only routing and cannot alone prove current coverage. Run `node scripts/verify-official-docs-coverage.mjs --check` so it compares against the official `llms.txt` and `sitemap.xml`; only after the check passes can you say the local inventory still covers the current official pages.
