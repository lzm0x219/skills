先定义 JavaScript 契约：`analyze(path, options)` 返回 Promise，并声明输入、返回报告和稳定、机器可读的错误形状。若已有领域 crate，将 napi-rs 适配层保持为薄绑定层，仅映射请求和结果，不复制领域规则。

验证时不创建包：先运行现有 Rust 测试，再在构建产物可用后用 Node 集成测试导入 addon，覆盖成功、无效输入、预期错误和异步完成路径。
