Define the JavaScript contract first: `analyze(path, options)` returns a Promise and declares inputs, the returned report, and a stable, machine-readable error shape. If a domain crate already exists, keep the napi-rs adapter as a thin binding layer that only maps requests and results without copying domain rules.

For verification, do not create a package: run existing Rust tests first, then after build artifacts are available import the addon with Node integration tests covering success, invalid input, expected errors, and async completion paths.
