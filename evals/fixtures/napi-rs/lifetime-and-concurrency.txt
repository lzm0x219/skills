Do not block the JavaScript main thread. Use AsyncTask, async fn, or ThreadsafeFunction as appropriate for the task from the official docs; background work should receive only owned Rust data (owned Buffer copies or already converted data).

Do not send or store Node-API handles, borrowed JavaScript values, or data outside their lifetime across threads. Tests should cover cancellation, callback failure, and final Node import; each claimed target also needs a runtime import test in a clean environment.
