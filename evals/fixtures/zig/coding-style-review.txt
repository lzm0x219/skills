First target the project's explicit Zig 0.15.2 version and check that version's language-reference Style Guide, standard library source, and `zig fmt`, then follow the repository's and adjacent code's public API conventions. Remove manual alignment that competes with the formatter. When the target version has no more specific rule, name types and callables that return a type with `TitleCase`, other callables with `camelCase`, and other values generally with `snake_case`; do not introduce breaking renames to stable APIs or external C names for style alone.

Use `///` documentation comments for public declarations and `//!` for module or container descriptions. Document ownership, the allocator, the releaser, errors, and lifetimes. Finally, review visibility, optional and error-union semantics, `defer`/`errdefer` cleanup, and whether tests actually execute. No files are modified in this response.
