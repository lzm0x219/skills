First confirm the project's pinned Zig version, dependency source, version or revision, license, and expected module. Use `zig fetch --help` from that target Zig version when checking command help. The `build.zig.zon` fields and hash rules are version-sensitive interfaces, so generate or verify the hash with the target toolchain rather than guessing it manually.

`zig fetch` may access the network, write a cache, or modify the manifest, so do not execute it in this response. After authorization and implementation, verify dependency resolution with an isolated or clean cache and then run the project tests. Preserve the user's global cache without deleting or contaminating it.
