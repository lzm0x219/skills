First record Zig 0.14.1, the target 0.16.0, every version-pinning location, and the CI and target matrices, then read the official release notes across that version span. List changes to the language, standard library, Build API, `build.zig`, `build.zig.zon`, C ABI, and tool options, and decide explicitly whether to support only the target version or retain a compatibility window.

Reproduce and classify errors with 0.16.0, then update toolchain pins, the manifest, build graph, and source in the smallest coherent increments. Follow with formatting checks, actual test execution, every target build, and runtime verification on available platforms. Preserve the old toolchain and change boundary so the migration can be rolled back after a failure. This response does not modify the project.
