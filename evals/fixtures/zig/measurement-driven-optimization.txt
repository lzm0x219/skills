Establish a baseline first: fix the Zig version, target, optimization mode, input dataset, warm-up procedure, machine load, and sampling method while recording the latency distribution and memory metrics. Use a profiler or equivalent evidence to locate the bottleneck instead of rewriting the parser from intuition.

Change one major variable at a time, repeat measurements under the same conditions, and report the distribution and variance while also running correctness and failure-path regressions. Claim an improvement only when the new results are consistently better than the baseline and the regressions pass. This response neither implements an optimization nor claims that optimization is complete.
