`unreachable` 不适合用户输入或其他外部输入中的未知 tag；应将它建模为普通错误或错误联合，只对已经证明的不变量使用 `unreachable`。

不要在整个 module 直接设置 `@setRuntimeSafety(false)`。先建立基准并 profile，确认安全检查是实测瓶颈；若确有收益，将关闭范围缩到有明确前置条件和测试保护的最小作用域。验证同时覆盖启用运行时安全检查的模式，以及项目实际支持的 ReleaseFast、ReleaseSmall 或其他 optimization mode，并记录非法输入行为。
