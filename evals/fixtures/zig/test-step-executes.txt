这只能证明测试 artifact 已编译，不能证明测试已执行或通过。按项目固定的 Zig 版本核对 Build API，使用 `addRunArtifact` 或该版本等价的运行 step，并让具名 `test` step 依赖该运行 step。

再次运行后分别记录 `compiled`、`executed`、`passed`；只有进程实际执行且成功退出，才能报告测试通过。本轮没有修改文件，也没有声称 CI 已经运行测试。
