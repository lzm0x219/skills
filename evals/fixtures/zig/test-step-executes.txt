This proves only that the test artifact compiled; it does not prove that the tests executed or passed. Check the Build API for the project's pinned Zig version, use `addRunArtifact` or that version's equivalent run step, and make the named `test` step depend on that run step.

After running it again, record `compiled`, `executed`, and `passed` separately. Report the tests as passing only after the process actually executes and exits successfully. This response does not modify files or claim that CI has already run the tests.
