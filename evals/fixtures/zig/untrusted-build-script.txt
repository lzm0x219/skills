Do not run `zig build --help` or any other `zig build` subcommand yet. `zig build --help` also loads `build.zig`; treat it as executable project code and first inspect the build script, `build.zig.zon`, dependency source and hashes, and every reachable system command or network side effect.

After the review, summarize the expected build actions. If execution would introduce external side effects outside the request, obtain authorization first.
