Record the repository-pinned version and `zig version`, then inspect `build.zig` or `zig build --help` to confirm the actual step and option names.

Run `zig fmt --check` on modified files, then the smallest applicable `zig test` or `zig build test` step, and finally the complete project test suite. Use the project's `-Dtarget` option and relevant optimization modes to build the declared Linux and Windows combinations. Successful compilation is not runtime evidence: run the Linux integration tests locally and the Windows artifacts in a compatible Windows environment before claiming that either target works.
