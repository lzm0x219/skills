Keep Zig 0.14.1 as the compatibility boundary. First compare the repository pin with `zig version`, then use the official 0.14.1 language reference and matching standard library or compiler source rather than applying a newer version's `build.zig` API.

Design only the static library artifact and its existing module wiring. Verify with the 0.14.1 compiler, repository tests, and the smallest build command. This response does not modify or migrate the project.
