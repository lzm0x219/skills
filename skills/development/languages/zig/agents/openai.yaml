interface:
  display_name: "Zig Development"
  short_description: "Build, debug, test, and review version-matched Zig projects"
  default_prompt: "Use $zig to design, implement, debug, test, or review this Zig project against its target version."
policy:
  allow_implicit_invocation: true
