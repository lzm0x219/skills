interface:
  display_name: "Bootstrap Project"
  short_description: "Create safe project scaffolds and development baselines"
  default_prompt: "Use $bootstrap-project to inspect this target, plan its baseline, and apply a supported conflict-free initialization."

policy:
  allow_implicit_invocation: false
