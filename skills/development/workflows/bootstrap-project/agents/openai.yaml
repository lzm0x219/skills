interface:
  display_name: "Bootstrap Project"
  short_description: "Plan safe project scaffolds and development baselines"
  default_prompt: "Use $bootstrap-project to inspect this target and prepare a safe initialization plan without writing files."

policy:
  allow_implicit_invocation: false
