import { describe, expect, test } from "vitest";

import { message } from "../src/index.js";

describe("message", () => {
  test("returns the CLI greeting", () => {
    expect(message()).toBe("Hello from {{PROJECT_NAME}}!");
  });
});
