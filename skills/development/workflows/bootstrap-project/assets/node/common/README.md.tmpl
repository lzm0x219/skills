# {{PROJECT_NAME}}

A minimal TypeScript {{PROJECT_SHAPE}} using ESM and Node.js.

## Development

```sh
mise install
mise run ci
```

Use `mise run format` to update formatting. The full quality gate checks the frozen pnpm lockfile, formatting, Oxlint, strict TypeScript, Vitest, and the emitted build.
