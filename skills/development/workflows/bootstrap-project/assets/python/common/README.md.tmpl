# {{PROJECT_NAME}}

A minimal Python {{PROJECT_SHAPE}} managed with uv.

## Development

```sh
mise install
mise run ci
```

Use `mise run format` to update Python formatting. The full quality gate checks the locked uv environment, Ruff, mypy, pytest, and package build.
