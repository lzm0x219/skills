# {{PROJECT_NAME}}

A minimal Rust {{PROJECT_SHAPE}} managed with mise and Lefthook.

## Development

Install the pinned tools and hooks:

```sh
mise install
```

Run individual tasks with `mise run format`, `format-check`, `lint`, `check`, `test`, or `build`. Run the complete local and CI gate with:

```sh
mise run ci
```
