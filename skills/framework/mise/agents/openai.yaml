interface:
  display_name: "mise environment management"
  short_description: "Manage tools, env, tasks, and reproducible mise envs"
  default_prompt: "Use $mise to design, configure, verify, or troubleshoot the project's mise development environment."
policy:
  allow_implicit_invocation: true
